# Independent E review

Root reciprocal review: read the delegated reference server collector, native wiring collector and async-aware stress collector and compared canonical raw tool responses/results. Verified explicit package prefixes, isolated native profiles, actual same-process storage races, acknowledgement-based loss counts, exact native note paths, bounded projection/read distinctions and all four Rig order results. Native sentinel responses contain actual successful content; weaker string predicates are not being accepted over error responses. No unresolved finding blocks this bounded evidence report. Native model invocation, durable race freedom and automatic retry safety are not established.

Reviewer: delegated Codex lab agent responsible for the reference/native-wiring and additional Basic concurrency collectors. Review of the root agent's Basic collector, upgrade collector, summarizer and report is independent; review of this reviewer's own collectors is not claimed as independent. No native model recall session was performed.

## Review scope and resolved findings

Read `scripts/spike-e-basic.mjs`, `scripts/spike-e-basic-upgrade.mjs`, `scripts/lab-mcp-probe.mjs`, `scripts/summarize-e.mjs`, and the report/ownership/security drafts. Examined the installed pinned read path to qualify DB-first storage, read caching and asynchronous Markdown materialization. Mutations, corruption, backup replacement and subprocess cleanup are contained within synthetic ignored run directories; no destructive-scope finding remains.

The root agent resolved these concrete findings before accepting the new canonical run:

- MCP shutdown now awaits actual child exit before SQLite copying/corruption.
- Retrieval and constrained-project predicates require expected positive content/identities; classified corruption errors replace generic failure-as-success.
- A failed distinct control can have a null projection: the summarizer now records failure without dereferencing null.
- The traversal absence check examines the escape directory, avoiding the incorrect lowercase filename assumption.
- Upgrade/reinstall reads reject protocol and structured tool errors before checking the sentinel.

The report correctly limits uninstall evidence to tested Markdown bytes plus later readable state, not exact SQLite/index preservation. It distinguishes quiescent full-state backup from Markdown projection export, and identifies the failed immediate-file stress attempt as a collector assumption error. The reference license notice and missing packaged LICENSE are qualified without inventing a blanket MIT license. Native-default policy is explicitly a simplicity judgment rather than measured recall superiority; integration/maintenance days are estimates.

## Canonical evidence review

Reviewed imported run [34839170081](https://github.com/serhii-baksheiev/agent-stack-lab/actions/runs/34839170081), scenario `79c251fb55d0fdded2ee2307dfe0856dbfe08870`. Executed `node scripts/validate-e-evidence.mjs`: 221 complete command records, 34 consistent snapshots, collector/probe hashes and ledger counts pass. Final aggregate is **153 passed / 16 failed / 14 unverified**. The report preserves negative and unverified outcomes rather than treating a green collector run as universal feature success.

Basic stress accepted 12/16 appends; four responses had structured indexing errors despite outer `isError: false`. No acknowledged marker was absent at the final bounded reads, no reader disagreement or projection timeout occurred, and all distinct-note controls passed. This supports the report's requirement to inspect structured errors and its refusal to claim race freedom or safe blind retries. The reference's per-round acknowledged/persisted name sets support 39 absent writes out of 96 acknowledgements (9/10/10/10); Windows observations are kept separate. Review of these own-collector outputs is a consistency check; root independently reviewed those collectors and outputs.

The diagnostic redaction predicate incorrectly required a retained sensitive key. Pinned upstream sources match installed file hashes and explicitly remove the whole key; the actual tool reads the raw config file at invocation. [Source-contract evidence](evidence/diagnostics/source-contract.json) records URLs, hashes and exact line references. Raw output exposes neither key nor canary, but no input-byte snapshot establishes the exact loaded canary, so the aggregate correctly records observed non-exposure separately and leaves exact redaction unverified. No rerun was needed to conceal or replace this failed lab predicate.

The final report's counts, measured concurrency limits, retained-state qualification and optional ADAPT recommendation align with reviewed evidence. No unresolved finding blocks acceptance of this bounded lab report. Semantic retrieval, native extraction/recall, private custom-memory migration, hostile same-account authorization and power-loss durability remain unverified. This approval does not authorize an unconditional concurrent-storage or redistribution claim.
