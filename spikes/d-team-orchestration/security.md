# Team security review

**Post-review incident:** full GET-repository evidence originally included a real API-supplied temporary clone credential in D and already merged B. Current files and collectors now use a five-field allowlist; a regression test and credential-field CI gate were added. Published history/PR-cache cleanup and token invalidation remain unresolved. See [incident record](../../docs/security-incidents.md). Earlier broad claims that no credentials entered evidence are withdrawn; the ordinary task/PR API arguments did not contain user-auth tokens.

The board collector checks exact repository identity and private visibility before writes and rejects API paths outside `serhii-baksheiev/agent-stack-lab`. Requests use the existing gh credential helper; no token value is read into code, arguments or evidence. Only synthetic issues, dependency links, PR comments/reviews and fixture branches are published inside this private lab. No Jira/RP or production repository is accessed for mutations.

Two API subprocesses share one authorized laboratory account. This proves competing API behavior, not identity separation or native model execution. Issue conditional PATCH fails explicitly; no unsafe assignment-as-lock claim is made. The Git ref experiment relies on force:false and linear ancestry. Expiry uses a cooperative client wall clock, and the publication guard is a read-check with a race window before arbitrary external side effects. An account with direct force-push/merge permission can bypass it. Do not describe this as hard fencing or production-safe exactly-once execution.

The stale-owner test attempts to advance only the synthetic claim ref; rejected commits remain unreferenced objects. The test does not deliberately merge stale production code. Crash is modeled by an absent owner after its claim subprocess ends and real TTL expiry, not an actual running model crash. Reviewer feedback is an own-account COMMENT review and corrective commit, not independent-account REQUEST_CHANGES or approval.

Cleanup closes and directly verifies every created issue/PR. Fixture worktrees, caches and installed upstream checkouts stay ignored. Retained synthetic remote branches support reproducible evidence and are never merged into master. Scope incidents from B remain disclosed in the final security review.
