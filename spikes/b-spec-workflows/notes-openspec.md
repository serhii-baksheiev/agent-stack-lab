# OpenSpec 1.13.0 manual-load trial

Pinned official npm package: `@fission-ai/openspec@1.13.0`, MIT, published tarball SHA-512 `1b1VbjELIUHPz8BLnkVxqPu4pAYS6BhfrLcmUIM7Bd0iJXQDvE+e/LBofE1kydegfxmG7R1xCMRXsl5VtZD+0w==` (verified locally). Registry metadata does not include gitHead; collector separately captures official release/tag commit evidence. Run `node scripts/spike-b-openspec.mjs` on Node 24 Ubuntu for the full isolated lifecycle. `--workflow-only` uses the already verified discovery installation at `.lab-runs/b-openspec-package` and does not run installation/collision scenarios. Never count workflow-only completion as full lifecycle evidence.

Actual installed CLI help includes init, update, status, instructions, new change, validate, archive, config, store, workset and JSON read surfaces. It exposes no uninstall or external issue-board export command. `update --force` refreshes generated instruction files; this is not package self-upgrade. Collector additionally installs official 1.12.0 and applies 1.13.0 update to an old initialized project. Delivery configuration (`config set delivery commands`, then update) tests actual generated-skill removal/reconciliation, not full uninstall. Read execution results before drawing collision conclusions.

The tracked fixture was initially drafted by the lab Codex agent from packaged spec-driven schema/templates, then reviewed against installed `.agents/skills/openspec-propose/SKILL.md` and `openspec-apply-change/SKILL.md`. The collector replays those authored artifacts in CLI dependency order; this is an artifact compatibility trial, not a claim that CLI instructions drove the original drafting chronology. Proposal is the brief, design records architectural choices, specs use native ADDED Requirements and four-level Scenario headings, tasks use native numbered checkboxes. Traceability and review are explicitly lab-authored additions. Native CLI generates initialization files, change metadata and instructions/status/validation outputs; it does not generate the filled feature artifacts or code. No native Claude inference or skill auto-discovery was tested.

The installed propose skill's planning boundary says “Do not start implementation in the same response” and demands a new user request even when the original asks to build. The standing laboratory instruction explicitly authorizes the complete manual-load implementation trial and has precedence. This trial therefore follows template/artifact/apply mechanics with a disclosed authorization adaptation; it is not evidence of strict end-to-end skill compliance. No synthetic approval is presented as a real owner's approval.

Restart probe invokes fresh CLI processes, requests task instructions twice, records byte equality and file hashes, then reads apply progress from checked tasks. Repeating `new change` with the same name must fail. This demonstrates durable filesystem state and read-only instruction generation; it does not measure a language model's duplicate task generation. `instructions apply --json` exposes task data for an external adapter. No board issues were published by this candidate agent.

Local authored implementation tests cover invalid types/syntax, frozen input, 254-character boundary, casing/whitespace deduplication, deterministic IDs, distinct records and serialized-state/fresh-import replay. Root's shared acceptance independently tests the same contract in a fresh process. Structural validation and task checkboxes do not establish semantic correctness by themselves.

Observed local workflow result: strict change validation reports one valid change and zero issues; apply reports `all_done` with four complete tasks; repeated task instructions are byte-identical and preserve file hashes; duplicate creation exits 1; archive and strict main-spec validation exit 0. Own Node tests pass 3/3. Full Rig lifecycle results remain separate. Official GitHub v1.13.0 resolves commit `9d4e5974e5c0d9a09b9c6c1e1eb0975e80ec4461`, dated 2026-09-09; this establishes tag identity and recent release activity, not an independent reproducible-build attestation tying those sources to npm bytes.

Material scope deviation: the first discovery npm install lacked a private package.json and explicit --prefix, so npm resolved the user-home parent and added 80 packages outside the lab. Targeted removal reported 80 removed; exact restoration is unverified because no before manifest/lock bytes were captured. Parent formatting/lock changes may remain. See evidence/openspec/scope-incident.json. Subsequent installs use an explicit isolated prefix. The full collector adds prefix assertions and isolated home/cache/config; no secrets or parent package contents are stored in evidence.
