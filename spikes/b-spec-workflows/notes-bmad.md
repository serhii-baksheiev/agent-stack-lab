# BMAD 6.12.0 trial

The registry package is MIT, with gitHead `05bfbd46d00766ec88eb9b42e76be2c575d64d7b` and SHA512 integrity recorded in evidence. The lifecycle collector verifies downloaded tarballs and pins published Rig 0.9.0. Exact runnable command from lab root: `node scripts/spike-b-bmad.mjs`. It creates unique ignored `.lab-runs/b-bmad-lifecycle-*` installations, isolates user/config/cache paths, captures real help, and tests both install orders, same-version updates twice, Rig repeat/dry-run/user-edit/deletion/upgrade, BMAD uninstall and Rig survival. Hosted results remain authoritative; merely writing the collector is not a passed lifecycle trial. A same-version update is not a cross-version release upgrade.

Actual inspected CLI commands are install, status and uninstall. Install supports `--action update` and `quick-update`; fresh noninteractive configuration needs `--modules bmm --tools claude-code,codex --yes`. Tool listing maps Claude to `.claude/skills`, Codex to `.agents/skills`. One local installation generated 29 skills for each provider. The Windows installer warned that uv was absent even though PowerShell resolved uv 0.12.5 and the required `uv run --no-cache .../render_skill.py` succeeded. This warning is a measured discrepancy, not evidence that build activation failed.

Current package uses the product brief, architecture spine, five-field SPEC kernel, memlog and build workflow; legacy story-oriented names are compatibility shims. The native headless spec skill forbids `stories.yaml` even on request, so this trial puts tasks and traceability in a companion instead of falsely attributing native story generation. Build step-02 routes this small, reversible, fully specified feature to oneshot. The rendered instructions and exact package source were read before implementing the module.

Tracked feature prose/code were authored by this Codex lab agent using those instructions and templates. CLI generated scaffolds only in the ignored installation. Shared upstream memlog.py made the canonical memory files; upstream lint_spine.py returned exit 0 and zero findings; five author-local Node tests passed. Native CLI status validates installation presence/versions, not spec correctness. Kernel coherence/preservation checks are explicitly manual model self-validation. No Claude process, paid inference, real owner approval or native skill auto-selection was exercised. Parent authorization supplies synthetic decisions and reserves commits for the consolidated PR. Root independently reviewed artifacts and implementation with no functional findings; nine shared checks pass, including a true separate-process restart. The build is marked done on that evidence, not invented native review dispatch.

Chronology qualification: skill/template files and the rendered build sequence were read before implementation, but the explicit config/customization resolver replay and some memlog reconciliation occurred after initial artifact drafting. The local exported folder layout is compacted for this tracked fixture; default brief/architecture output directories were not reproduced exactly. This demonstrates manual adaptation of the conventions, not conformance to every native activation/persistence step. The required semantic reviewer gate remains distinct from the passing deterministic spine linter.

The same-slug no-change audit preserves CAP IDs and byte-identical SPEC output after rereading the memlog. This measures a manual re-derivation decision, not automatic semantic idempotency. The append-only memlog changes on each audit by design. The local test's cache-busted import is supplemented by root's truly separate-process restart acceptance.

No dedicated board-export CLI was advertised in help. Brief/architecture customization supports external_handoffs directives naming configured MCP tools, but defaults are empty and no board export was executed. A custom handoff is an integration surface, not a verified native exporter. Scope and governance byte counts come from measured snapshots; this tiny task does not establish value on larger projects.

Hosted run `34834437236` completed both orders on Linux/Node v24.20.0. Native renderer/linter/tests/status/update/uninstall all returned 0. No preexisting files changed when the second component was installed. Rig dry-run was read-only, all three shared-file edits and the deleted Rig file survived upgrade, and uninstall preserved Rig-claimed files plus authored artifacts. BMAD overwrote the edited generated skill; repeated update changed four config/manifest files on both repetitions. Combined Rig+BMAD installation measured 534 project files and 4,341,348 bytes in Rig-first (4,341,364 in BMAD-first). This is project footprint, not prompt tokens. Collector source hash matches hosted provenance. Cross-version BMAD upgrade remains unverified.
