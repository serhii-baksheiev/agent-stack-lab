# Authorized history cleanup

The owner approved rewriting only five lab branches on 2026-09-14. An atomic push used explicit force-with-lease for each previously recorded tip. The local mirror-backup must be retained until separately authorized for deletion.

Fresh clone verified exactly **1,948 unique historical JSON blobs across every branch** at the post-rewrite heads recorded in the scan, all parsed, no credential-field findings (an independent re-scan on 2026-09-14 at the later heads counted 1,994 blobs, still zero findings). A/F/C commits and trees are identical. Broad candidate keys were design tokens, file names/hashes and package dependency names; no credentials. Across GitHub PR refs there are **1,949** JSON blobs: PR9/head and PR10/head still reach the original B credential. Both original B and superseded D content remain accessible by old SHA. The pre-rewrite Actions artifact `spike-b-evidence` (run 34835217413) also contained the original response; it was deleted on 2026-09-14 and no other evidence artifact contained a nonempty credential field (34 artifacts scanned). Invalidation is unverified. This is not complete GitHub erasure.

See [commit map](commit-map.txt), [branch push map](initial-push.json), [fresh scan](fresh-clone-scan.json), [cache probes](cache-and-pattern-audit.json), [PR comparison](pr11-before-provenance.json), and [translated evidence index](evidence-reference-index.json). Historical SHA identifiers in the security incident narrative deliberately identify the exposed original commits.

Commit references in normalized evidence identify the corresponding cleaned source. Historical API/command responses are normalized records, not new executions or byte-identical originals; the index records before/after hashes and mappings. Old workflow URLs are retained only as historical provenance; run 34841316242 executed on original commit `4fbe889…`, not on its mapped identity. Exact-head validation on the rewritten tree: Spike D runs [34845116484](https://github.com/serhii-baksheiev/agent-stack-lab/actions/runs/34845116484) (`3f1e4fb`) and [34846314339](https://github.com/serhii-baksheiev/agent-stack-lab/actions/runs/34846314339) (`b363efa`), laboratory validation [34846252532](https://github.com/serhii-baksheiev/agent-stack-lab/actions/runs/34846252532) (`b363efa`) and master push [34846249366](https://github.com/serhii-baksheiev/agent-stack-lab/actions/runs/34846249366) (`d22d425`). Synthetic real-board writes were not repeated.

Known translation artifact: losing/stale lease proposals (`ed7f07b5…` in `14-lease-propose-actor-b.json`, `67276494…` in `22-stale-owner-proposal.json`) were never rewritten because no branch references them, so their live GitHub objects still carry the original parents (`c30f2de…`, `78c0fdd…`) while the normalized records show translated parents. The translated parent is a mapping annotation, not the object's content; this also demonstrates that the original master commit `c30f2de…` still exists on GitHub as an unreferenced object.
