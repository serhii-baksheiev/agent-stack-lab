# Local subscriptions — synthetic design

Classification: new standalone fixture, architectural under Superpowers' path
rules. This document is authored by the Codex laboratory agent after reading
pinned Superpowers 6.3.0 brainstorming instructions, not generated by its CLI.

Authority: [shared feature brief](../../../../feature-brief.md), including
the exact validation rule `^[^\s@]+@[^\s@]+\.[^\s@]+$` and `sub-N` ID expression.

Problem/brief: prevent duplicates in the caller-owned state behind the C
subscription-form design. The authoritative feature brief fixes B-R1–B-R6.

Options: a pure function, a mutable singleton, or persistent service. Choose the
pure function: explicit state enables restart and avoids needless dependencies.
The singleton fails independence; a service exceeds the authorized feature.

Design: `subscribe(state,email)` validates/normalizes before mutation, reuses an
existing record on duplicates, otherwise returns a new array and deterministic
ID. No I/O, random values, hidden global state, or copying C's implementation.

Tests cover normalization, invalid input, frozen state, duplicate/distinct
records, exact length boundary and a new-process JSON resume. Reference visual
context remains the separate C design fixture; this trial delivers the domain
module only.

Approval limitation: the upstream brainstorming skill requires actual human
approval. The owner instead explicitly authorized autonomous laboratory choices.
The synthetic brief supplies decisions; it is NOT evidence that a real human
approved this generated design. This is an adapted manual-load trial, not a
successful unmodified headless Superpowers workflow.

Self-review: no unresolved scope choices, placeholder requirements, external
service assumptions or missing function contract. Independent review remains a
separate check before the laboratory PR is merged.
