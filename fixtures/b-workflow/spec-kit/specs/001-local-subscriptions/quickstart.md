# Quickstart and continuation

Read spec.md, plan.md, tasks.md and the shared feature brief. Run
`node --test tests/subscriptions.test.mjs` inside this fixture. From lab root,
run `node scripts/spike-b-acceptance.mjs spec-kit` for independent acceptance.
For a new session, use task IDs below and the actual test/evidence state; do not
infer completion from a prior chat. The caller may persist returned state as JSON.
