## 1. Define the change

- [x] 1.1 Record brief, architecture and scenarios for B-R1 through B-R6; verify proposal.md, design.md and specs/local-subscriptions/spec.md against the common brief.

## 2. Implement the transition

- [x] 2.1 Implement normalization, validation, duplicate lookup and immutable creation for B-R1 through B-R4; verify Node tests for invalid input, normalized identity and deterministic distinct IDs.
- [x] 2.2 Verify B-R5 serialization and fresh-import duplicate handling using the restart Node test.

## 3. Review delivery

- [x] 3.1 Record B-R6 traceability and review; verify each requirement has code or artifact and a check, run Node tests and OpenSpec strict validation/status in the collector.
