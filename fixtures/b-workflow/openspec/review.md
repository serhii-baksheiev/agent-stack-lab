# Manual-load Codex review

Authorship: the lab Codex agent authored the domain implementation and all tracked feature documents. Structure follows the installed OpenSpec 1.13.0 spec-driven templates; these filled artifacts are not CLI-generated prose or evidence of native Claude inference. The executable collector records actual CLI-generated instructions and skeleton metadata separately, then copies this reviewed fixture into its ignored working project.

Chronology: initial drafting used the packaged schema/templates; installed propose/apply skills were read afterward and used for review. CLI instructions/status subsequently replayed the authored artifacts in dependency order. Standing user authorization covers implementation despite the propose skill's stricter stop-after-planning convention; this is a disclosed manual-load adaptation.

The valid-state precondition matches the brief. Validation precedes any state construction. Duplicate identity is retained. New transitions allocate only a record and array; input objects receive no writes. Source contains no imports, clocks, random values, network, storage or mutable globals. The restart test imports a new module URL after JSON serialization. Exact 254-character acceptance and overlong rejection are covered. Traceability includes non-code B-R6.

Acceptance commands: `node --test fixtures/b-workflow/openspec/test/subscriptions.test.mjs`; `node scripts/spike-b-openspec.mjs` records actual OpenSpec strict validation/status and repeat instruction hashes. Test outputs, not these task checkboxes, determine acceptance. The change remains active for inspection; archive is exercised only in the ignored collector project.

No real stakeholder approval, live Figma, native Claude skill selection or native task-board export is claimed. Product decisions are inherited from the synthetic brief. Instructions generation is not task-content generation; repeated CLI instructions are measured independently from authored tasks.
