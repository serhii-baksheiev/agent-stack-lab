# Synthetic fixtures

Use fictional projects and data only. Installed tool payloads and runtime data belong in ignored `.lab-runs/`; commit only small authored fixtures, scripts and sanitized evidence.
